/**
 * Command families are computed by the Rust replay kernel.
 *
 * The former TypeScript family classifier was comparator-only and was removed in
 * S8. `/clearance allow` uses the small authoring helper in
 * `command-family-primitives.ts`; no runtime replay path imports this module.
 */
export {};
