[package]
name = "clearance-node"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
clearance-core = { path = "../clearance-core" }
napi = { version = "3.11.0", default-features = false, features = ["napi4"] }
napi-derive = "3.6.0"
serde_json = "1"

[build-dependencies]
napi-build = "2.3.2"
