/** Terminal-safe semantic glyphs shared by status renderers. */
export const GLYPHS = {
  /** Turn count, as `↻5≤30`. */
  turns: "↻",
  /** Session compaction count. */
  compactions: "⇊",
} as const;
